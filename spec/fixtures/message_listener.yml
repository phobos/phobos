logger:
  file: log/phobos.log
  level: fatal

listeners:
  - handler: Phobos::Handler
    topic: test
    start_from_beginning: true
    group_id: test-1
    max_concurrency: 1
    delivery: message
